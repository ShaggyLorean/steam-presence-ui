<Window x:Class="SteamPresenceUI.Views.SteamLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Login to Steam" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        ResizeMode="CanResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#252525" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔒" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock x:Name="UrlDisplay" Text="Navigating..." 
                               Foreground="#AAAAAA" FontSize="13" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Please login first!" Foreground="#FFD700" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button x:Name="ExtractButton" Content="✅ Extract Cookies &amp; Close"
                            Background="#007ACC" Foreground="White" FontWeight="Bold"
                            Padding="16,8" Click="ExtractButton_Click"
                            BorderThickness="0" FontSize="13"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- WebView2 Browser -->
        <wv2:WebView2 x:Name="SteamBrowser" Grid.Row="1"/>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#252525" Padding="12,8">
            <TextBlock x:Name="StatusText" Text="Log in to your Steam account, then click 'Extract Cookies &amp; Close'." 
                       Foreground="#888888" FontSize="12"/>
        </Border>
    </Grid>
</Window>

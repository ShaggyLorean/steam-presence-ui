<Page x:Class="SteamPresenceUI.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Foreground="White"
      Title="Settings">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="General Settings" FontSize="28" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,16"/>
        
        <!-- WinUI 3 Style Settings Card -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8" Padding="16">
            <StackPanel>
                <!-- Start with windows toggle -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Run when Windows starts" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="Launch Steam Presence UI in background when you log in." Foreground="Gray" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                    <ui:ToggleSwitch x:Name="StartupToggle" Grid.Column="1" Checked="StartupToggle_Checked" Unchecked="StartupToggle_Unchecked"/>
                </Grid>

                <Grid x:Name="StartMinimizedRow" Visibility="Collapsed" Margin="24,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Start Minimized" FontWeight="SemiBold" FontSize="13"/>
                        <TextBlock Text="Launch hidden in tray when Windows starts." Foreground="Gray" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                    <ui:ToggleSwitch x:Name="StartMinimizedToggle" Grid.Column="1" Checked="StartMinimizedToggle_Changed" Unchecked="StartMinimizedToggle_Changed"/>
                </Grid>

                <Separator Margin="0,16"/>

                <!-- Auto-start engine toggle -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Auto-Start Engine" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="Run the presence engine automatically when the app launches." Foreground="Gray" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                    <ui:ToggleSwitch x:Name="AutoStartEngineToggle" Grid.Column="1" Checked="AutoStartEngineToggle_Checked" Unchecked="AutoStartEngineToggle_Unchecked"/>
                </Grid>

                <Separator Margin="0,16"/>

                <!-- Excluded Games -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                        <TextBlock Text="Excluded Games" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="Comma separated list of Game Names or Steam IDs to ignore (e.g. Counter-Strike 2, 730). Fallback to Discord native RPC." Foreground="Gray" FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                    <ui:TextBox x:Name="ExcludedGamesInput" Grid.Column="1" PlaceholderText="Game1, Game2..." TextChanged="ExcludedGamesInput_TextChanged" VerticalAlignment="Center"/>
                </Grid>
                
                <Separator Margin="0,16"/>
                
                <!-- Open Config Button -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Advanced Configuration" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="Edit config.json file directly to change API Keys and User IDs." Foreground="Gray" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                    <ui:Button Grid.Column="1" Content="Open config.json" Icon="{ui:SymbolIcon DocumentEdit24}" Click="OpenConfig_Click"/>
                </Grid>
            </StackPanel>
        </Border>

    </Grid>
</Page>
